const db = require('../../mysqlConnect');
var sha256 = require('js-sha256');

async function register(req, res) {
  let query = `INSERT INTO eleves (ideleve, ) VALUES ()`;
}
exports.register = register;
